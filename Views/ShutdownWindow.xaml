<!-- ============================================================
     HeliVMS - 智慧影像管理系統     禾秝軟體開發團隊 / 代碼設計：洪俊士 / 版本：V1.0.0
     ============================================================ -->
<Window x:Class="HeliVMS.Views.ShutdownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HeliVMS 關機中" Width="640" Height="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        Background="{DynamicResource BackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Colors.xaml"/>
                <ResourceDictionary Source="/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="2" CornerRadius="8">
        <Grid Margin="40,36">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="智慧影像管理系統"
                       Foreground="{DynamicResource TextBrush}"
                       FontSize="22" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Text="檔案回存中"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,24"/>

            <ProgressBar Grid.Row="2" IsIndeterminate="True"
                         Height="8" Foreground="{DynamicResource PrimaryBrush}"
                         Background="{DynamicResource SurfaceBrush}"
                         Margin="0,0,0,20"/>

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <TextBlock x:Name="StatusText"
                           Text="正在停止 ffmpeg 錄影程序…"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="14"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
