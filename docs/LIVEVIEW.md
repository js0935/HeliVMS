# 即時監看介面規劃與規範（Live View）

版本：v1.4（M14 引言）
範圍：HeliVMS 主監看視窗（MainWindow）之即時顯示、互動與警報功能。
對應章節：架構 §3 監看、§5 事件引擎、§8 UI。

## 1. 功能清單

### 1.1 佈局與切換
- [x] 單一畫面（1 格）
- [x] 2 × 2 四路
- [x] 3 × 3 九路、4 × 4 十六路（M9：格子以程式動態建立，最小格約 320px 寬依視窗縮放）
- [x] 單格展開 → 全螢幕（M13：F11 展開/還原、ESC 退出；右鍵「切換全螢幕」同源：`ToggleExpandCellAsync`）
- [x] 佈局記憶：重開沿用上次分割（M9：寫入資料根 `ui.json`）

### 1.2 每格顯示
- [x] 畫面影像（Stretch=Uniform，按格比例等比縮放）
- [x] 狀態文字：未連線／連線中／重連中／即時監看
- [x] 錄影中徽章（全域 REC）
- [x] AI 疊加框（M7）：person＝紅、vehicle＝藍＋confidence 標籤；框與格同步縮放；**僅在「AI 疊加」開啟且該格 DC 啟用時繪製**
- [ ] 每格頻道名＋解析度（後續）→ 頻道名徽章已完成（M12：每格左下角半透明徽章，錄影中加 ●REC）；解析度後續
- [x] 每格頻道名徽章＋錄影指示（M12）
- [ ] 運動觸發時格框閃動（後續）

### 1.3 互動
- [x] 頻道下拉選單連線
- [x] 一鍵連線／錄影／回放／事件中心按鈕
- [x] 每格右鍵選單（M7）：全螢幕切換、該頻道錄影切換、開啟事件中心
- [x] 點擊選格 → 該格詳情側欄（M12：「頻道總覽」側欄，顯示格→頻道對應、狀態、REC、最後 AI 時間；點列/點格雙向同步選取，高亮邊框）

### 1.4 即時警報
- [x] 底部警報列（M7）：即時推播 AI 偵測（人/車＋confidence）與離線/重連，點擊定位到該格；最多顯示最近 3 筆
- [x] 未確認事件計數徽章（M7）：定時自 alarm_events 統計並顯示於警報列右側
- [x] 警報列點擊跳轉（M13：點擊警報列跳至最後一筆警報對應格並高亮）

## 2. 行為規範

- **不阻塞**：AI 推理、事件寫入、警報推播均不得運行於 UI/解碼執行緒；監看幀渲染只做最後一次取樣結果的拷貝更新。
- **AI 疊加**：
  - 僅於「AI 疊加」開關啟用且畫面格有資料流時繪製。
  - 座標為 0..1 正規化，繪製時乘以該格顯示區解析度。
  - 疊加框使用該格「最新推理結果」；幀到達時以當格畫面對齊繪製（滯後物件位置可容忍至推理周期內）。
  - 類別色彩：person＝番茄紅、car/bus/truck/motorcycle/bicycle＝水藍；他人類別以灰框。
  - label（類別＋confidence）僅在單一畫面佈局顯示；四路只畫框。
  - **每格 AI 策略（M14）**：多格佈局僅「選中格」推理並繪製疊加；單格展開時取消間隔降為 200ms；非選中格停用取樣（零 CPU）。
- **布點對應**：Cell→Channel 對應由 ChannelManager 連線次序決定；重連後保持一致。
- **佈局切換**：格子為程式動態建立（UniformGrid，每格＝影像＋AI 疊加＋狀態文字＋右鍵選單），切換即重建；已連線的 cell 對應（`_cellChannel`）與既得位圖保留，重建後自動套回。
- **佈局記憶**：切佈局時以 JSON 寫入資料根 `ui.json`（鍵 `layout`，0..3）；下次啟動套用。
- **警報**：同一頻道在同 3 秒內之相同事件類型節流為一筆；推播格式 `HH:mm:ss 頻道名｜事件詳情`。
- **未確認計數**：週期 15 秒自 alarm_events 統計 unacknowledged；點擊「事件」開啟事件中心時即時重新統計。

## 3. 效能規範

| 項目 | 目標 |
| --- | --- |
| 推理不阻塞監看 | < 方案：獨立佇列＋worker，取樣 ≥ 400ms |
| 監看渲染 | 單幀拷貝 ≤ 66ms（15fps） |
| AI 疊加繪製 | 每格 ≤ 4ms，且僅接收幀事件時觸發 |
| 警報列 | 不需 DB 查詢（除未確認計數週期） |
| 32 路策略（v1.4 M14） | 僅選中/展開格開啟 AI（非選中停用省 CPU）；單格 200ms、≤4路 400ms、≥9路 800ms 降頻；`AiConcurrencyScheduler` 全域限併發（預設 2，`HELIVMS_AI_CONCURRENCY` 可調） |

## 4. 驗收標準
1. 四路同時連線虛擬流，開啟 AI 疊加：人身/車流樣本畫面即時出現對應框與 label。
2. 畫格縮放（視窗 resize／切佈局）後框與影像仍對齊。
3. 警報列出現 AI 偵測與離線/重連訊息。
4. 時間軸無阻塞（錄影持續、UI 可操作）。
5. 全方案建置 0 warning；單元測試覆蓋偵測轉發、count API。
6. E2E `liveoverlay`：RTSP 車流樣本 → 收集偵測流，確認 bbox 於畫內合理性（0≤x+y<1、w/h>0）。

## 5. 非目標（本次）／後續
- 每格解析度顯示、運動格閃框（頻道名徽章與詳情側欄已完成於 M12）
- 音訊、PTZ、數位變焦（警報點擊跳轉與 F11 快捷鍵已完成於 M13）