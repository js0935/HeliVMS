<Window x:Class="HeliVMS.Dialog.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="匯出錄影片段" Height="520" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Colors.xaml"/>
                <ResourceDictionary Source="/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="匯出錄影片段"
                   FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="開始"
                       VerticalAlignment="Center" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <DatePicker x:Name="StartDatePicker" Grid.Row="0" Grid.Column="1" Margin="0,0,4,4" FontSize="12"
                        Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
            <TextBox x:Name="StartTimeBox" Grid.Row="0" Grid.Column="2" Text="00:00:00" FontSize="12" Margin="0,0,0,4"
                     Style="{StaticResource ModernTextBox}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="結束"
                       VerticalAlignment="Center" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <DatePicker x:Name="EndDatePicker" Grid.Row="1" Grid.Column="1" Margin="0,0,4,0" FontSize="12"
                        Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
            <TextBox x:Name="EndTimeBox" Grid.Row="1" Grid.Column="2" Text="23:59:59" FontSize="12"
                     Style="{StaticResource ModernTextBox}"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="輸出位置" VerticalAlignment="Center" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <TextBox x:Name="OutputPathBox" Grid.Column="1" FontSize="12" Padding="4,3" Margin="4,0,4,0"
                     Style="{StaticResource ModernTextBox}"/>
            <Button Grid.Column="2" Content="瀏覽..." Padding="8,3" FontSize="11"
                    Click="Browse_Click"
                    Style="{StaticResource SecondaryButton}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="格式" VerticalAlignment="Center" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <ComboBox x:Name="FormatCombo" Grid.Column="1" Width="120" FontSize="12"
                      Style="{StaticResource ModernComboBox}"
                      SelectedIndex="0">
                <ComboBoxItem Tag="mp4">MP4 (H.264)</ComboBoxItem>
                <ComboBoxItem Tag="avi">AVI</ComboBoxItem>
            </ComboBox>
        </Grid>

        <Border Grid.Row="4" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderBrush}"
                Padding="0,0,0,4" Margin="0,0,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="選擇攝影機" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="CameraCountText" Text="0 台"
                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                               VerticalAlignment="Center" Margin="6,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="全選" Style="{StaticResource SecondaryButton}"
                            Height="20" FontSize="9" Padding="6,0" Margin="0,0,4,0"
                            Click="SelectAllCameras_Click"/>
                    <Button Content="清除" Style="{StaticResource SecondaryButton}"
                            Height="20" FontSize="9" Padding="6,0"
                            Click="ClearAllCameras_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="5" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent" BorderThickness="0">
            <StackPanel x:Name="CameraListPanel" Margin="0,0,0,4"/>
        </ScrollViewer>

        <TextBlock x:Name="ProgressText" Grid.Row="6" Text=""
                   FontSize="12"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Margin="0,4,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button x:Name="ExportBtn" Content="開始匯出" Width="100" Height="34"
                    Click="Export_Click"
                    Style="{StaticResource ModernButton}"/>
            <Button Content="取消" Width="80" Height="34" Margin="8,0,0,0"
                    Click="Cancel_Click"
                    Style="{StaticResource SecondaryButton}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
