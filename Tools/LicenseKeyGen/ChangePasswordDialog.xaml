<Window x:Class="LicenseKeyGen.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="變更密碼"
        Width="360" Height="300"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        MouseDown="Window_MouseDown">
    <Border Background="{StaticResource BgDarkBrush}" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Border Grid.Row="0" Background="{StaticResource BgCardBrush}" CornerRadius="8,8,0,0"
                    MouseDown="TitleBar_MouseDown">
                <Grid>
                    <TextBlock Text="變更密碼"
                               Foreground="{StaticResource TextBrush}"
                               FontSize="12" VerticalAlignment="Center"
                               Margin="12,0,0,0"/>
                    <Button Width="32" Height="32" HorizontalAlignment="Right"
                            Background="Transparent" BorderThickness="0"
                            Click="Cancel_Click"
                            Foreground="{StaticResource SecondaryTextBrush}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <TextBlock Text="✕" FontSize="14"
                                               Foreground="{TemplateBinding Foreground}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                        <Setter Property="Background" Value="#C0392B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="20,8,20,20">

                <TextBlock Text="🔑 變更密碼"
                           FontSize="18" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>

                <TextBlock Text="目前密碼" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="OldPasswordBox" Style="{StaticResource PasswordBoxInput}"
                             FontSize="14" Padding="8,6" Margin="0,0,0,10"/>

                <TextBlock Text="新密碼" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource PasswordBoxInput}"
                             FontSize="14" Padding="8,6" Margin="0,0,0,10"/>

                <TextBlock Text="確認新密碼" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Style="{StaticResource PasswordBoxInput}"
                             FontSize="14" Padding="8,6" Margin="0,0,0,10"/>

                <TextBlock x:Name="StatusText" Style="{StaticResource FieldLabel}"
                           HorizontalAlignment="Center" Margin="0,4,0,4"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="取消" Style="{StaticResource CopyButton}"
                            Margin="0,0,6,0" Click="Cancel_Click"/>
                    <Button Grid.Column="1" Content="確認變更" Style="{StaticResource PrimaryButton}"
                            Margin="6,0,0,0" Click="Confirm_Click"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
