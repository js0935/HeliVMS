<UserControl x:Class="HeliVMS.Controls.PTZControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="200">
    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1" CornerRadius="8"
            Padding="12">
    <StackPanel>
        <TextBlock Text="PTZ 控制" Foreground="{StaticResource TextBrush}"
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <!-- PTZ 方向搖桿 -->
        <Grid Width="140" Height="140" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="1" Content="▲"
                    Style="{StaticResource SecondaryButton}"
                    Click="Up_Click"/>
            <Button Grid.Row="1" Grid.Column="0" Content="◀"
                    Style="{StaticResource SecondaryButton}"
                    Click="Left_Click"/>
            <Button Grid.Row="1" Grid.Column="2" Content="▶"
                    Style="{StaticResource SecondaryButton}"
                    Click="Right_Click"/>
            <Button Grid.Row="2" Grid.Column="1" Content="▼"
                    Style="{StaticResource SecondaryButton}"
                    Click="Down_Click"/>
        </Grid>

        <!-- Zoom 控制 -->
        <TextBlock Text="縮放" Foreground="{StaticResource SecondaryTextBrush}"
                   FontSize="11" Margin="0,4,0,4"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="＋ 拉近"
                    Style="{StaticResource SecondaryButton}" Margin="0,0,4,0"
                    Click="ZoomIn_Click"/>
            <Button Grid.Column="1" Content="－ 拉遠"
                    Style="{StaticResource SecondaryButton}" Margin="4,0,0,0"
                    Click="ZoomOut_Click"/>
        </Grid>

        <!-- 預設點 -->
        <TextBlock Text="預設點" Foreground="{StaticResource SecondaryTextBrush}"
                   FontSize="11" Margin="0,12,0,4"/>
        <WrapPanel x:Name="PresetsPanel" Margin="0,0,0,4"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="＋ 新增" Click="SavePreset_Click"
                    Style="{StaticResource SecondaryButton}" Margin="0,0,4,0"/>
            <Button Grid.Column="1" Content="－ 刪除" Click="DeletePreset_Click"
                    Style="{StaticResource SecondaryButton}"/>
        </Grid>
    </StackPanel>
        </Border>
</UserControl>
